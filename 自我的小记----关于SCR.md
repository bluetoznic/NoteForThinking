# 关于 SCR 的思考

> 关于传统视觉定位、ACE、GLACE 与 R-SCoRe 的学习记录。

## 传统视觉定位与隐式三角化

关于传统视觉定位方法，大多数输入一组图片，找特征点，显存三维地图（SFM），即显式三角化，然后特征提取网络获得每个三角点的描述子，然后现在输入一张图片，提取像素描述子，做特征匹配，然后 pnp-ransac。

### DSAC 与 SAC：从显式到隐式三角化

![传统视觉定位与 ACE 示意图](Pasted%20image%2020260919155035.png)

### ACE：隐式三角化

               他执行的是一个隐式三角化的过程，他要的对一个像素预测在当前世界坐标系的三维坐标，在训练时他只有image和pose，经过一个预训练的encoder+解析坐标的mlp,然后由于相邻像素的差异性很小，梯度回归很慢，所以这里引入了随机打乱buffer训练的机制加强训练，这样保证了又快又高效的网络。
               对于坐标来看，只有pose和img，这时候loss就用预测出来的y反投影去算像素距离（刚开始y很离谱，慢慢的收敛），GLACE 作者对 ACE 成功原因的核心解释就是：相似输入倾向于产生相似输出，使相似观测的 reprojection losses 被共同最小化，从而产生三角化。
               > **补充：** 单个二维像素拟合一个 3D 点时，由于缺乏深度，射线上任意一点都可以产生反投影误差，因此存在无数种深度可能。这看起来是一个**欠定问题**，但是多组二维像素经过共享参数网络可以拟合同一个 3D 点，构成隐式三角化，共享 loss 的下降使得这个 3D 点变得唯一。
         但是这种共享参数共同输出，是保持不变性的体现，面对比如相似像素，比如两块窗户但不同位置，这个时候不变性反而害了它，因此需要一种不同的强有力约束（**来自Glace**）。
             > **我的思考：** 我现在还没看 GLACE 是怎么做的。如果让我思考，要怎么引入强有力的约束呢？比如不同光照下，同种像素层级怎么约束出来？加大样本量？但是仍然无法在推理时面对同种光照纹理条件下的分辨问题。引入旁侧视野约束或许是一个方法：扩大视野覆盖，将同层像素的邻居信息加入 loss？不过每张视角不一定都能覆盖到旁侧的标志物，后续继续看 GLACE 原文。
         好了，glace提出了扩大感受野，这确实对不同像素有效，但是如果是同一点，模型又会因为过强约束导致作弊，loss下降，迫使模型泛化变差.......，我们的想法失败了，继续往下看：
              ![GLACE 感受野示意图](Pasted%20image%2020260919164212.png)
          本质还是把视野扩大，但不是引入周围像素，而是对图片获得一个全局特征（vggt的思想类似，全局token和图片内token）
            ![全局特征示意图](Pasted%20image%2020260919165628.png)
         全局特征，但是两张图某个像素块就一定满足共视？
              作者在这里分析了两者的必要关系，纯在一定的概率关系，但并非一定。
         因此，即使两幅图像实际上存在共视关系，它们的 global feature distance 仍然可能相当大。因为图像只会存在部分重叠，不能说全局描述子相似。
         > **进一步思考：** 那要不把全局描述子放入一个 cluster 进行硬编码？一方面，每个 cluster 中必须拥有足够多的同一三维点观测，从而能够进行三角化；另一方面，每个 cluster 又不能过大，否则其中会出现大量具有歧义的 local encoding。
         本质来说这个global特征如果太确定，会变得容易区分，又回到上面说的模型作弊不找到约束，简单的收敛loss，如果让特征变得模糊呢？
         这时候对特征加一个小范围扰动，**Feature Diffusion→Implicit Grouping**

---

## 2026-09-20：继续思考 R-SCoRe

### SCR 的继续思考：R-SCoRe
      思考SCR的四大核心：
       （ACE 因为局限在小场景，没有考虑全局，但 GLACE 引入了全局编码。）

![ACE 与 GLACE 对比示意图](Pasted%20image%2020260921173424.png)
     重新核实网络要素： **训练时已知 RGB + Pose + Intrinsics，测试时给 RGB + Intrinsics，要求 Pose。**
     ![R-SCoRe 网络要素示意图 1](Pasted%20image%2020260921174927.png)
     ![R-SCoRe 网络要素示意图 2](Pasted%20image%2020260921175058.png)
     这篇论文最精彩的部分3.3来了，因为这部分对特征进行了重新思考，首先是局部特征：
     ![局部特征示意图](Pasted%20image%2020260922090611.png)
     然后是全局特征：
     **全局编码从 GLACE 的“视觉外观描述”转变成了 R-SCoRe 的“几何共视拓扑描述”：先根据相机位姿和视锥重叠建立共视图，再通过 Node2Vec 将这种图拓扑关系编码成低维全局向量。**
     **这就是本文的创新，即把视觉的欺骗性换成更有约束力的，基于朝向，fov，位置等等硬性约束，让全局特征具有几何性质。**
     后面全文又做了一个增强，glace利用高斯扰动，让特征变成一个范围，失去确定性，这里做的更绝，即把graph中具有共视关系的特征进行替换，让比如局部特征A和全局特征B结合，这样相比小尺度扰动更有力。
     最后进入loss阶段，作者观察到，距离相机近的重投影误差相比远的大，为了让他们处于同一层级，作者这里进行了图像层面的std归一化，然后换tanh，而是用gemanloss：
     ![损失函数示意图](Pasted%20image%2020260922151216.png)
