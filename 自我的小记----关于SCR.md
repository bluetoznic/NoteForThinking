# 关于 SCR 的思考

> 关于传统视觉定位、ACE、GLACE 与 R-SCoRe 的学习记录。

## 传统视觉定位与隐式三角化

关于传统视觉定位方法，大多数输入一组图片，找特征点，显存三维地图（SFM），即显式三角化，然后特征提取网络获得每个三角点的描述子，然后现在输入一张图片，提取像素描述子，做特征匹配，然后 pnp-ransac。

### DSAC 与 SAC：从显式到隐式三角化

![传统视觉定位与 ACE 示意图](Pasted%20image%2020260919155035.png)

### ACE：隐式三角化

ACE 执行的是一个隐式三角化过程：它为每个像素预测当前世界坐标系中的三维坐标。训练时只有 image 和 pose，模型经过预训练的 encoder 与坐标回归 MLP 进行学习。由于相邻像素的差异很小，直接回归的梯度下降较慢，因此引入随机打乱 buffer 的训练机制，以提高训练效率。

对于坐标回归，模型只有 pose 和 image 作为输入，loss 则通过预测坐标的反投影误差计算像素距离。训练初期预测坐标较不准确，随着训练逐渐收敛。GLACE 对 ACE 成功原因的核心解释是：相似输入倾向于产生相似输出，使相似观测的 reprojection loss 被共同最小化，从而形成隐式三角化。

**补充：** 单个二维像素拟合一个 3D 点时，由于缺乏深度信息，射线上任意一点都可能产生较小的反投影误差，因此存在无数种深度可能。这看起来是一个**欠定问题**，但是多组二维像素经过共享参数网络后，可以拟合同一个 3D 点；共享 loss 的下降会使这个 3D 点逐渐确定。

### 不变性的限制

共享参数共同输出体现了模型的不变性，但也可能带来问题。例如，两块外观相似但位置不同的窗户，可能被模型映射成相似结果。这种情况下，不变性反而削弱了定位能力，因此需要更强的约束。

**我的思考：** 如果要引入强有力的约束，可以考虑不同光照下的像素关系、旁侧视野信息或邻居 loss。例如，窗户旁边可能是树木，也可能是墙壁，可以将这些上下文作为附加约束。不过，并不是每个视角都能覆盖旁侧标志物，因此这种约束仍然存在适用范围。

### GLACE：扩大感受野

GLACE 提出了扩大感受野的方法。这对区分不同像素有效，但如果针对同一个点施加过强约束，模型可能通过投机方式降低 loss，导致泛化能力下降。

![GLACE 感受野示意图](Pasted%20image%2020260919164212.png)

它并不是简单地引入周围像素，而是从图像中提取全局特征，思想上类似 VGGT 中的全局 token 与图像内 token。

![全局特征示意图](Pasted%20image%2020260919165628.png)

### 全局特征与共视关系

问题在于：两张图像中的某个像素块是否一定满足共视关系？作者分析认为，两者之间存在概率关系，但并非必然成立。即使两幅图像实际上存在共视关系，它们的 global feature distance 仍然可能相当大，因为图像通常只有部分重叠，不能简单地认为全局描述子一定相似。

### 进一步思考：隐式分组

可以考虑将全局描述子放入 cluster 中进行硬编码。一方面，每个 cluster 中需要拥有足够多的同一三维点观测，从而完成三角化；另一方面，cluster 又不能过大，否则会出现大量具有歧义的 local encoding。

如果全局特征过于确定，模型会变得容易区分，又回到通过简单收敛降低 loss、却没有真正学习约束的问题。或许可以对特征加入小范围扰动，即 **Feature Diffusion → Implicit Grouping**。

---

## 2026-09-20：继续思考 R-SCoRe

### SCR 的继续思考：R-SCoRe
      思考SCR的四大核心：
       （ACE 因为局限在小场景，没有考虑全局，但 GLACE 引入了全局编码。）

![ACE 与 GLACE 对比示意图](Pasted%20image%2020260921173424.png)
     重新核实网络要素： **训练时已知 RGB + Pose + Intrinsics，测试时给 RGB + Intrinsics，要求 Pose。**
     ![R-SCoRe 网络要素示意图 1](Pasted%20image%2020260921174927.png)
     ![R-SCoRe 网络要素示意图 2](Pasted%20image%2020260921175058.png)
     这篇论文最精彩的部分3.3来了，因为这部分对特征进行了重新思考，首先是局部特征：
     ![局部特征示意图](Pasted%20image%2020260922090611.png)
     然后是全局特征：
     **全局编码从 GLACE 的“视觉外观描述”转变成了 R-SCoRe 的“几何共视拓扑描述”：先根据相机位姿和视锥重叠建立共视图，再通过 Node2Vec 将这种图拓扑关系编码成低维全局向量。**
     **这就是本文的创新，即把视觉的欺骗性换成更有约束力的，基于朝向，fov，位置等等硬性约束，让全局特征具有几何性质。**
     后面全文又做了一个增强，glace利用高斯扰动，让特征变成一个范围，失去确定性，这里做的更绝，即把graph中具有共视关系的特征进行替换，让比如局部特征A和全局特征B结合，这样相比小尺度扰动更有力。
     最后进入loss阶段，作者观察到，距离相机近的重投影误差相比远的大，为了让他们处于同一层级，作者这里进行了图像层面的std归一化，然后换tanh，而是用gemanloss：
     ![损失函数示意图](Pasted%20image%2020260922151216.png)
