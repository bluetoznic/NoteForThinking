-Glace代码:
      ==训练细节和策略：==
          AMP和GradScaler：
               ==autocast：决定哪些计算用半精度==
               ==GradScaler：保护半精度反向传播==   
         自适应坐标：
            不使用位置解码器：
                 mean = [1, 3, 1, 1]
                 每个像素共享同一个三维均值
             使用位置解码器：
                 mean = [B, 3, H, W]
                 每个像素根据特征计算自己的三维参考中心
```
train_ace.py
    ↓
TrainerACE.__init__()
    ↓
TrainerACE.train()
    ├── create_training_buffer()
    │   ├── Encoder 提取局部特征
    │   ├── 生成像素坐标
    │   └── 缓存到 GPU
    │
    ├── run_epoch()
    │   ├── 随机采样缓存
    │   └── 调用 training_step()
    │
    └── save_model()
```

-RScore
      ==关于pipeline-----why not glace？==
    `原始 GLACE 的训练对象只有一个核心任务：

```
输入图像
 → 提取特征
 → 训练场景坐标回归头
 → 保存地图

因此 `TrainerACE` 直接管理所有组件：
TrainerACE
 ├── Dataset
 ├── Encoder
 ├── Regressor
 ├── Training Buffer
 ├── Optimizer
 ├── Loss
 ├── Training Loop
 └── Checkpoint

例如它在 `__init__()` 中直接创建：
self.dataset = CamLocDataset(...)
self.regressor = Regressor(...)
self.optimizer = optim.AdamW(...)


之后 `train()` 直接调用：

self.create_training_buffer()
self.run_epoch()
self.training_step()


这种设计适合以下情况：

- 项目规模较小
- 只有一种主要训练流程
- 模型、数据和 loss 的组合比较固定
- 不需要频繁替换数据集或模型
- 主要目标是快速复现论文结果

如果此时额外引入：
Trainer → Pipeline → DataManager → Model
反而可能增加跳转和理解成本。
```
 
```
 为什么 RScore 要引入 pipeline

RScore 基于更通用的训练框架设计，可能需要支持：

- 多种模型
- 多种数据管理器
- 多种训练方式
- 不同参数组
- 验证和推理
- 分布式训练
- checkpoint 恢复
- callbacks
- TensorBoard / WandB
- 混合精度
- 多种 sampler

因此它把职责拆开：
Trainer
 └── 负责训练流程和生命周期

Pipeline
 └── 连接数据和模型

DataManager
 └── 负责数据集、DataLoader、training buffer

Model
 └── 负责前向传播、metrics 和 loss

Optimizers
 └── 负责 optimizer、scheduler 和梯度更新

Config
 └── 负责组件组合和超参数


`Pipeline` 的核心价值不是“训练模型”，而是把：
datamanager
model
组织成一个统一接口，让 `Trainer` 不必知道具体模型和数据细节。

例如 Trainer 只需要调用：
loss, loss_dict, metrics_dict = self.pipeline.get_train_loss_dict(step）

而不用关心内部是：
GLACE 数据集
还是 NeRF 数据集
还是 Node2Vec 数据集
```

一个通用的架构是：
```
project/
├── configs/
│   ├── model_config.py
│   ├── data_config.py
│   └── experiment_config.py
├── data/
│   ├── datasets/
│   ├── dataloaders/
│   └── datamanagers/
├── models/
│   ├── encoder.py
│   ├── decoder.py
│   └── model.py
├── losses/
│   └── losses.py
├── trainers/
│   └── trainer.py
├── pipelines/
│   └── pipeline.py
├── optimizers/
│   └── optimizers.py
├── evaluation/
│   └── evaluator.py
├── utils/
│   ├── logging.py
│   ├── checkpoint.py
│   └── visualization.py
└── scripts/
    ├── train.py
    └── evaluate.py
```

常见调用流程为：
```
train.py
  ↓
读取配置
  ↓
创建 Trainer
  ↓
Trainer.setup()
  ├── 创建 DataManager
  ├── 创建 Model
  ├── 创建 Optimizer
  └── 创建日志和 checkpoint
  ↓
Trainer.train()
  ├── 取 batch
  ├── 前向传播
  ├── 计算 loss
  ├── 反向传播
  ├── 更新参数
  ├── 验证
  └── 保存模型
```

==三种架构风格：==

单体式结构：
```
     ```
Trainer
 ├── 数据
 ├── 模型
 ├── loss
 ├── optimizer
 └── 训练循环
 代表：原始 GLACE。

优点：
- 代码直接
- 运行路径容易追踪
- 适合论文复现和小型项目
缺点：
- Trainer 容易变得很大
- 替换模型或数据集比较困难
- 多种实验之间容易产生重复代码
```

模块化 Trainer
```

Trainer
 ├── DataManager
 ├── Model
 ├── Loss
 └── Optimizer

Trainer 只负责编排。
优点：
- 职责清晰
- 容易测试和扩展
- 适合中型项目
缺点：
- 文件数量更多
- 调用链更长
- 初学者需要理解多个接口
```

 Pipeline + Framework
```
Trainer
 └── Pipeline
      ├── DataManager
      ├── Model
      ├── Loss
      └── Evaluation
代表：当前 RScore / scrstudio 风格。
优点：
- 可以统一多种实验
- 适合大型研究框架
- Trainer 不依赖具体模型
- 方便分布式训练和插件式扩展
缺点：
- 抽象层较多
- 对小项目可能显得复杂
- 出问题时需要沿着多层调用链定位